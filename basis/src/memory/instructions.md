The listing above is this workspace's memory: notes kept from earlier
sessions, one Markdown file per memory. When a memory's description bears on
the task, read its file. When you learn something durable and worth having
next session — a fact about this project, a preference the user stated, a
correction you were given, reference material that was hard to assemble —
keep it: write a new Markdown file in a directory the listing draws from, or
edit the existing file of that name to revise it. Open the file with YAML
frontmatter naming `name`, a one-line `description`, and `type` (one of
`user`, `feedback`, `project`, `reference`), then write the body free-form.
