fn main() {
    // Placeholder entry point. Modes (per docs/ARCHITECTURE.md):
    //   lan                  -> ACP server on stdio (default; the front door)
    //   lan run "<prompt>"   -> headless one-shot, JSONL events with --json
    //   lan watch "<prompt>" -> recurring headless runs
    eprintln!("lan: not yet implemented; see docs/ARCHITECTURE.md");
    std::process::exit(1);
}
